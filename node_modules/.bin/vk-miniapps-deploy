#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@vkontakte/vk-miniapps-deploy/bin/vk-miniapps-deploy" "$@"
  ret=$?
else 
  node  "$basedir/../@vkontakte/vk-miniapps-deploy/bin/vk-miniapps-deploy" "$@"
  ret=$?
fi
exit $ret
